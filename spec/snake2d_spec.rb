# frozen_string_literal: true

RSpec.describe Snake2d do
  it "has a version number" do
    expect(Snake2d::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
